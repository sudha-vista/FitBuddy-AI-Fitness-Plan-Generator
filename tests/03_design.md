# Phase 3 – Project Design

Architecture: Browser → FastAPI → service layer → Gemini/SQLite → Jinja2. Components: Jinja2 frontend, FastAPI/Pydantic backend, Google GenAI SDK, SQLite/SQLAlchemy. Core routes are the four document-specified HTML routes plus REST endpoints.