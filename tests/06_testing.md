# Phase 6 – Project Testing

Run `pytest -q`. Automated tests cover the home page, health API, plan creation, user retrieval, feedback update, duplicate ID validation and HTML form submission. Tests use mock AI mode and do not consume Gemini quota.