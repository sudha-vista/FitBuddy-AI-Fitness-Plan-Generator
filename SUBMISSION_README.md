# FitBuddy Final Submission Package

Includes the FitBuddy source code and eight phase-wise project documents.

Excluded for security/cleanliness:
- `.env` / API credentials
- local SQLite database files
- Python virtual environment
- Python cache files
- Git metadata

Use `.env.example` to create a local `.env` file when running the project.
